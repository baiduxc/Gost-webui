# 更新记录

## 1.6.3

- 移除页面顶部右侧的四行装饰文字及其残留样式。

## 1.6.2

- 修复重复安装时按默认端口检查旧配置，导致服务已启动却误报“未检测到面板监听”的问题。
- 安装器改为按 systemd 主进程识别实际监听端口，等待面板就绪后再放行防火墙并输出访问地址。
- 精简系统缺少端口探测工具时不再误报失败；服务异常会直接打印最近日志，无需再手动执行排障命令。

## 1.6.1

- GOST 落地机部署命令改为保留换行与缩进的代码块，便于逐段复制执行。
- 节点管理页移除右侧详情卡片，节点列表改为完整宽度；点击节点名称仍可查看详细信息。
- 面板访问设置改为只填写端口号，后端自动转换为监听地址并兼容旧配置。
- 提升页头文字的字重与对比度，移除品牌图标，并统一将“幽谷灵境”改为“Gost面板”。

## 1.6.0

- 中转机可直接设为本机落地，代理服务由面板现有 GOST 进程统一管理。
- GOST 体系从固定 Shadowsocks 扩展为 HTTP、HTTP/2、SOCKS4/4A/5、Shadowsocks TCP/UDP、SNI、Relay。
- 代理协议与传输通道独立组合，加入 TLS、WebSocket、gRPC、KCP、QUIC、HTTP/3、WebTransport、SSH、ICMP 等通道。
- 远程落地按通道自动选择 TCP/UDP 透传，并生成对应 YAML、systemd 与临时运行命令。
- 保持旧版 GOST Shadowsocks 节点兼容，通用订阅可输出 GOST 原生客户端地址。

## 1.5.0

- 全新仙侠主题：蓝天云海、悬浮仙山背景，墨绿与浅玉两套组件配色。
- 横向导航、节点选择与详情面板、直接启停开关，保留复制链接、订阅和其他节点操作。
- 统一登录、概览、通知、设置及弹窗样式，优化手机布局和键盘操作。
- 背景图片本地打包；亮暗模式均不叠加全页颜色滤镜。
- 修正图表边缘日期裁切和零流量进度显示。
- 统一源码与发布构建版本，修复链接器版本号覆盖。
