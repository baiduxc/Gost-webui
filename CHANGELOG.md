# 更新记录

## 1.8.1

- 修复：系统设置「订阅与证书」保存/签发/清除后即时重拉配置并刷新，新增「当前生效的证书与私钥」只读展示区（手动证书回显存储值，ACME 读取缓存，含复制按钮）
- 优化：顶栏恢复滚动磨砂效果且全宽铺满视口（内容仍按页宽居中对齐）；LOGO+标题可点击回概览
- 新增：系统设置可配置站点标题（默认 Gost-WebUI），登录页/浏览器标签/页脚同步生效
- 新增：数据备份支持「下载备份 / 选择备份文件 / 导入恢复」三步，恢复前自动另存当前库为 panel.db.pre-restore，导入后面板自动重启
- 修复：REALITY 表单 UUID 字段占位文案与按钮位置不符（新增行内「生成」按钮）


## 1.8.0

- 新增：**VLESS + REALITY 节点**（sing-box 引擎），与 gost 并存互不影响
  - 添加节点新增「VLESS + REALITY」标签页：伪装域名 + 一键生成密钥对/UUID/ShortID
  - 每个节点由独立 sing-box 实例承载（进程级精确流量统计），开关/删除即时拉起/回收
  - 支持每日/每月/总量配额：超限自动停实例，恢复周期自动拉起（限速与并发暂不支持）
  - 订阅同时输出 vless://reality 链接与 Clash Meta 配置，兼容 v2rayN / NekoBox / Shadowrocket
  - 未安装 sing-box 的服务器不显示该标签页；install.sh 与 Docker 镜像自动附带 sing-box

## 1.7.2

- 修复：「客户端连接」通知刷屏——旧逻辑按 gost 累计连接数增量发送且未走冷却去重，任何 TCP 短连接（心跳/探测/重连）都会触发推送。
- 调整：改为仅在节点在线人数 0↔N 状态翻转时各发一条「客户端已连接节点 / 客户端已全部断开」，事件改名「客户端上下线」，并纳入冷却去重（同节点同类事件默认 600 秒内不重发）。

## 1.7.1

- 修复：概览与节点详情的流量曲线支持鼠标/触屏感应，悬停显示该时间点上行、下行与合计数据卡。
- 修复：节点详情切换 24 小时/7 天/30 天后错误跳回 7 天的问题。
- 调整：节点详情弹窗加宽；客户端链接与二维码从详情移至「订阅」弹窗顶部「客户端连接」区。
- 新增：通知事件「客户端连接」——节点迎来新客户端时推送（节点名/新增数/当前在线数）。
- 新增：Docker 部署支持——多阶段 Dockerfile（面板 + 源码编译 gost，含 vmess）、GHCR 自动构建工作流（amd64/arm64）、docker-compose 与容器入口脚本、README 安装章节。

## 1.7.0

- 界面全面重构为北欧极简风格（亮/暗双模）：中性浅灰底、近实心小圆角卡片、轻量磨砂、细字重大数字，绿色仅用于强调与主操作；移除旧主题、皮肤叠加层与背景插画。
- 全新线描 W 标识与 SVG 站点图标（favicon.svg），品牌区去除边框底色。
- 顶栏导航绝对居中、文字精确居中；节点操作下拉菜单修复定位错乱（右缘对齐、越界翻转、脱离 backdrop-filter 坐标系）。
- 节点列表重排版：名称/配额分列层级化，状态列仅保留开关（超限暂停除外）；编辑节点时额度与单位下拉合并为一行。
- 概览与全部页面数据轮询间隔统一为 5 秒；主机负载卡片进度条带过渡动画。
- 移动端（390px）系统设置长路径不再横向溢出；全站细滚动条、Toast 底部胶囊化。
- gost 日志级别可在系统设置中开关（off 时输出重定向空设备并清空旧日志），日志文件权限收紧为 0600，附 logrotate 配置。
- 新增 `GET /api/backup`：以一致性快照方式下载面板数据库（bbolt WriteTo）。
- 登录 Cookie 在 TLS 环境下自动加 Secure 标记。
- 安装/卸载脚本补齐：卸载时清理 go-ui 残留、~/.gost、防火墙规则；GitHub 下载支持 GH_PROXY 加速。
- 页脚更新：左侧显示实时版本号，右侧署名跳转仓库。

## 1.6.3

- 移除页面顶部右侧的四行装饰文字及其残留样式。

## 1.6.2

- 修复重复安装时按默认端口检查旧配置，导致服务已启动却误报“未检测到面板监听”的问题。
- 安装器改为按 systemd 主进程识别实际监听端口，等待面板就绪后再放行防火墙并输出访问地址。
- 精简系统缺少端口探测工具时不再误报失败；服务异常会直接打印最近日志，无需再手动执行排障命令。

## 1.6.1

- GOST 落地机部署命令改为保留换行与缩进的代码块，便于逐段复制执行。
- 节点管理页移除右侧详情卡片，节点列表改为完整宽度；点击节点名称仍可查看详细信息。
- 面板访问设置改为只填写端口号，后端自动转换为监听地址并兼容旧配置。
- 提升页头文字的字重与对比度，移除品牌图标，并统一将“幽谷灵境”改为“Gost面板”。

## 1.6.0

- 中转机可直接设为本机落地，代理服务由面板现有 GOST 进程统一管理。
- GOST 体系从固定 Shadowsocks 扩展为 HTTP、HTTP/2、SOCKS4/4A/5、Shadowsocks TCP/UDP、SNI、Relay。
- 代理协议与传输通道独立组合，加入 TLS、WebSocket、gRPC、KCP、QUIC、HTTP/3、WebTransport、SSH、ICMP 等通道。
- 远程落地按通道自动选择 TCP/UDP 透传，并生成对应 YAML、systemd 与临时运行命令。
- 保持旧版 GOST Shadowsocks 节点兼容，通用订阅可输出 GOST 原生客户端地址。

## 1.5.0

- 全新仙侠主题：蓝天云海、悬浮仙山背景，墨绿与浅玉两套组件配色。
- 横向导航、节点选择与详情面板、直接启停开关，保留复制链接、订阅和其他节点操作。
- 统一登录、概览、通知、设置及弹窗样式，优化手机布局和键盘操作。
- 背景图片本地打包；亮暗模式均不叠加全页颜色滤镜。
- 修正图表边缘日期裁切和零流量进度显示。
- 统一源码与发布构建版本，修复链接器版本号覆盖。
